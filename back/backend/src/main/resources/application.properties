spring.application.name=backend
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
server.port=${PORT:8080}

# JWT
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Paginacion
spring.data.web.pageable.default-page-size=20

# Upload de archivos
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
app.upload.dir=${APP_UPLOAD_DIR:uploads}
app.base-url=${APP_BASE_URL:http://localhost:8080}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://sofia-couture-frontend.onrender.com}

# Email — Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:sofia1278zamura@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:npfibizncbacdleo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
app.mail.from=${APP_MAIL_FROM:Sofia Couture EC <sofia1278zamura@gmail.com>}
app.mail.admin=${ADMIN_EMAIL:sofia1278zamura@gmail.com}
app.mail.resend.api-key=${RESEND_API_KEY:}
app.mail.resend.api-url=${RESEND_API_URL:https://api.resend.com/emails}

# Payphone
payphone.token=${PAYPHONE_TOKEN:}
payphone.store-id=${PAYPHONE_STORE_ID:}
payphone.prepare-url=https://pay.payphonetodoesposible.com/api/button/Prepare
payphone.confirm-url=https://pay.payphonetodoesposible.com/api/button/V2/Confirm
payphone.response-base-url=${PAYPHONE_RESPONSE_BASE_URL:http://localhost:5173}

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
